apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: default
  labels:
    app: api-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: api-service
---
apiVersion: v1
kind: Service
metadata:
  name: gmail-service
  namespace: default
  labels:
    app: gmail-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: gmail-service
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: default
  labels:
    app: ai-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: ai-service
---
apiVersion: v1
kind: Service
metadata:
  name: rule-engine-service
  namespace: default
  labels:
    app: rule-engine-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: rule-engine-service
---
apiVersion: v1
kind: Service
metadata:
  name: meeting-service
  namespace: default
  labels:
    app: meeting-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: meeting-service
