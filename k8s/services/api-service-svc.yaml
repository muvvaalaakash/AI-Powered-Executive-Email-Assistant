apiVersion: v1
kind: Service
metadata:
  name: api-service-svc
  namespace: aeroinbox
  labels:
    app: api-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: api-service
