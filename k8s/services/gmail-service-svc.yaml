apiVersion: v1
kind: Service
metadata:
  name: gmail-service-svc
  namespace: aeroinbox
  labels:
    app: gmail-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: gmail-service
