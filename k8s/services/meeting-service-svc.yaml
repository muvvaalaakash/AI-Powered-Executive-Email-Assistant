apiVersion: v1
kind: Service
metadata:
  name: meeting-service-svc
  namespace: aeroinbox
  labels:
    app: meeting-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: meeting-service
