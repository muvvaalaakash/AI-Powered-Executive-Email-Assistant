apiVersion: v1
kind: Service
metadata:
  name: rule-engine-service-svc
  namespace: aeroinbox
  labels:
    app: rule-engine-service
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: rule-engine-service
